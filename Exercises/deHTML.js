/**
 * Remove the html tags from an HTML string
 */

const input = '<div><br><p>Hello</p></div>';

function deHTML(html) {

}

console.log(deHTML(input));
// Expected Output:
// div br p Hello -p -div